EL reto nos proporiciona un codigo en python y el output de ejecutar dicho codigo.
Vemos que el codigo lo que hace es importar la flag en bytes y convertirla al entero en decimal de esos bytes en big endian. Por lo que simplemente tenemos que pasar el entero a bytes de nuevo en big endian. (Importante a la hota de pasar el entero a bytes la funcion "to_bytes" necesita la longitud en bits del entero pero como lo estamos pasando a bytes esta longitud tiene que cuadrar, tiene que ser multiplo de 8 que es la longitud de un byte por lo que sumando 7 bits y dividiendo entre 8 nos aseguramos de que la longitud sea adecuada)
